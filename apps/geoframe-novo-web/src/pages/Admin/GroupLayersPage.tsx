// Gestão de publicações/group-layers.
import { Stack, Text, Title } from '@mantine/core';

export function GroupLayersPage() {
  return (
    <Stack gap="xs">
      <Title order={3}>Grupo de camadas</Title>
      <Text c="dimmed">Em breve.</Text>
    </Stack>
  );
}
