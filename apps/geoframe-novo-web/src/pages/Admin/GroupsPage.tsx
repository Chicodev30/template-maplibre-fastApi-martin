// Gestão de grupos.
import { Stack, Text, Title } from '@mantine/core';

export function GroupsPage() {
  return (
    <Stack gap="xs">
      <Title order={3}>Grupos</Title>
      <Text c="dimmed">Grupos do Keycloak. Em breve.</Text>
    </Stack>
  );
}
